{
  "name": "@zora/worker",
  "version": "0.0.0",
  "private": true,
  "description": "ZORA background worker — hold/reservation sweeps + payment reconciliation. Singleton (advisory-lock guarded).",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@zora/core": "workspace:*",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2"
  }
}
