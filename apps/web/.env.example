# ZORA web (Next.js)
# Backend origin the /api/* proxy + middleware forward to.
API_URL=http://localhost:4101

# White-label root domain (tenant subdomain detection in middleware).
ZORA_ROOT_DOMAIN=zora.com
