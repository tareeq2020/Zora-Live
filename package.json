{
  "name": "zora",
  "version": "0.0.0",
  "private": true,
  "description": "ZORA monorepo — Next.js web + NestJS api + Expo mobile. Migration in progress; see MIGRATION-PLAN.md.",
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "legacy:site": "npm --prefix legacy/zora-site start"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
