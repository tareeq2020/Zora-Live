{
  "name": "@zora/core",
  "version": "0.0.0",
  "private": true,
  "description": "ZORA framework-agnostic domain core (postgres.js data layer, domain logic). No HTTP/Nest imports.",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "db:test:xbridge": "node --test test/xbridge.test.mjs"
  },
  "dependencies": {
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2"
  }
}
