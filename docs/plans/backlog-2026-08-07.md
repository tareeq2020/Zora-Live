# Backlog — 2026-08-07

Consolidates the 10 items raised in this session's review pass with the 4 items
still open from the prior roadmap. Grouped into lanes by how they should ship,
not by who raised them.

## Lane 0 — Ship now, no review needed (bug fixes + trivial copy)

| # | Item | Root cause / change | Size |
|---|---|---|---|
| 0.1 | **Discover hides the only real event in its own city** | `apricot-crush.city = "Dar Es Salaam"` (free text) vs discover's fixed `CITIES` ids (`dar`, `zanzibar`, `nairobi`, `accra`, `lagos`). Turn drop-editor's CITY field from free text into a select bound to the same fixed list discover uses, so drift is structurally impossible. Backfill the 1 real + 5 seed rows. | S |
| 0.2 | Signup handle placeholder | `dashboard/signup/page.tsx` — change placeholder example from `thebrunchcity` to `organizer` | XS |
| 0.3 | Hide KULTUR banner | Discover — flag off / comment out for now | XS |
| 0.4 | Hide About + Commission pages | Unpublish (redirect to home or 404), not delete — trivially reversible. Keep Help. | XS |
| 0.5 | Add Instagram to Help contact card | `zora.pass` — help-app.tsx contact card, alongside email/phone already added in BS46 | XS |
| 0.6 | "Get tickets at {subdomain}" copy | `discover-app.tsx:788` → `GET TICKET AT ' + sheetEv.subdomain.toUpperCase()`. Change to `Get tickets at {Organizer Name} store` — drop the raw subdomain string. Check the matching `.su` subdomain chip in `storefront/[handle]/events/[id]/page.tsx` too. | XS |

**Recommend bundling 0.1–0.6 as one PR today** — none of them touch shared/risky code, all mechanically verifiable.

## Lane 1 — Straightforward builds, no design review needed

| # | Item | Notes | Size |
|---|---|---|---|
| 1.1 | **Per-organizer theme (real bug)** | Move theme fields onto the `organizer` table (migration) instead of the global `collection_store` singleton. `ThemeController` GET takes `?handle=`, falls back to `DEFAULT_THEME` only for unset fields; PUT scoped to the acting organizer (guard same as other org-scoped writes). Update the 3 web callers (`storefront/[handle]/page.tsx`, `storefront/[handle]/events/[id]/page.tsx`, `dashboard/storefront/studio/page.tsx`) to pass handle. | M |
| 1.2 | "No booking fee" copy audit | Found in 13 files. NOT a blanket delete — `DESIGN.md`'s current canon ("No booking fees — our cut comes from organizers, not you") is still true and can stay; what has to go is anything implying the *gateway* takes nothing. Auditing each occurrence individually: storefront ×3, events pages ×2, drop-editor, home-app, marketing page, discover ×2, commission (being hidden anyway), help, checkout-flow. | S–M |
| 1.3 | Replace fake-chat bubble with WhatsApp CTA | Keep ZBot's canned FAQ flow (genuinely useful triage), but change the "talk to a human" terminal leaf + the empty-state fallback from "we'll email you back" to a `wa.me/255741099989` deep link. | S |
| 1.4 | Payment-method admin toggle *(carried over)* | Confirmed greenfield — no such toggle exists. New `settings.methodsEnabled` (mobile/billpay/card), admin panel control (extends the existing BS29 Payments-routing panel), checkout honors it. | M |
| 1.5 | Checkout: method-first, then network, with logos *(carried over)* | Split into two literal steps (method screen, then network screen when mobile is chosen) instead of both on one screen; swap the 4 text network buttons for the vendored logos (`apps/web/public/logos/mobile-money/*.png`, committed in BS46). | S–M |
| 1.6 | Archive demo-seed events | Offshore, Basement ×2, Palmwine ×2 — mechanical via the existing admin archive tool, **pending your confirmation on which orgs to archive** (this is a content decision, not a bug). | XS, blocked on you |
| 1.7 | "Explore events" prominence *(carried over from #6)* | Layout tweak on `coming-soon.tsx` home hero — link already points to `/thebrunchcity`, just needs visual weight under the moving graphic. | XS |

## Lane 2 — Needs a short design pass before building

| # | Item | Notes | Size |
|---|---|---|---|
| 2.1 | Super-admin "mega event" picker | New admin control (Events section) to flag one event as featured; discover reads the flag and renders it pinned above the per-city grid. Needs a quick layout call: does the pinned card replace the city grid's top slot, or sit in its own strip above the city switcher? | M |
| 2.2 | Bottom CTA icon fix | Discover's organizer-acquisition strip (~line 723, "Launch a custom Zora subdomain...") — need to see the actual icon in browser before deciding swap vs removal; flagged as misleading Google-signin iconography. | XS–S |
| 2.3 | Signup → consumer plane | `/dashboard/signup` currently uses the control-room dark palette; move it to the Consumer plane (aura gradient, Space Grotesk, home page's cinematic language) since it's a public acquisition funnel, not an internal tool. The palette itself is already locked in `DESIGN.md` — this is applying existing tokens to a new surface, not inventing new ones, so a full `/plan-design-review` is probably overkill; a quick visual check against DESIGN.md's Consumer plane spec should suffice. | M |

## Lane 3 — Biggest lift: needs `/plan-design-review`

| # | Item | Notes | Size |
|---|---|---|---|
| 3.1 | Sitewide dark mode | What you're calling "the night-mode toggle" doesn't exist as a control anywhere — discover is just permanently dark. The real ask is: dashboard/checkout/storefront are still on the light "paper" palette while discover/home/scan are dark. This is the **same item as the long-pending "unify organizer dashboard to dark" work** from the original #9 design review that never got executed. Recommend folding both into one workstream: build a real site-wide dark/light mechanism and use it to finally finish the dashboard unify. | L |

## Still open, unscoped (from the prior message, not re-raised today)

- **#3 — thin analytics MVP.** Scope not yet confirmed (bare checkout-started→paid from `order` table vs. the fuller page_view+hash version).
- **Admin default password rotation** — flagged twice, no go-ahead yet.
- **Messaging composer UI** (organizer-facing) + **admin Broadcasts placeholder** — backend/API/worker shipped in BS45, UI never built.

## Recommended sequencing

1. **Lane 0 today** — one PR, all mechanical, ships the fix for the live "real event invisible" bug immediately.
2. **Lane 1 in parallel lanes** — same cadence as before (branch off develop → e2e → PR → merge → promote → mirror), can run 1.1/1.2/1.3 and 1.4/1.5 as two independent lanes.
3. **Lane 2** after a quick look at 2.1/2.2 in browser and applying 2.3 from the already-locked palette.
4. **Lane 3** (dark mode + dashboard unify) gets its own `/plan-design-review` pass, same as the original admin port — this is the one item here big enough to warrant it.

## GSTACK REVIEW REPORT

Not yet run. This document is the scoping pass; Lane 3 is the only item recommended for `/plan-design-review` before implementation. Lanes 0–2 are small enough to build directly and verify against `DESIGN.md`'s existing locked tokens.
